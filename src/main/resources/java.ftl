public class ${className}{
    private Integer ${id.name};
    public ${id.type} ${id.getName}(){
        return ${id.name};
    }
    public void ${id.setName}(${id.type} ${id.name}){
        this.${id.name}=${id.name};
    }
}